version: '3.8'

services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./logs:/app/logs   # CSV/JSONの永続化   
      - ./data:/app/data   # 画像の永続化
    environment:
      - ENV=production
